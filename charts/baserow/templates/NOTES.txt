1. Get the frontend URL by running these commands:
{{ include "baserow.frontend.publicUrl" . }}

2. Get the backend URL by running these commands:
ASGI: {{ include "baserow.backend.publicUrl" . }}{{ .Values.backend.ingress.paths.asgiPath }}
WSGI: {{ include "baserow.backend.publicUrl" . }}{{ .Values.backend.ingress.paths.wsgiPath }}

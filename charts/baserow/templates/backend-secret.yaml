{{- if not .Values.backend.config.existingSecret -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "baserow.backend.secretName" . }}
data:
  jwt-signing-key: {{ .Values.backend.config.jwtSigningKey | b64enc }}
  secret-key: {{ .Values.backend.config.secretKey | b64enc }}
{{- end }}

{{- if .Values.celery.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "baserow.celery.fullname" . }}
  labels:
    {{- include "baserow.celery.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "baserow.celery.fullname" . }}
  minReplicas: {{ .Values.celery.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.celery.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.celery.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.celery.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.celery.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.celery.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "headwind-mdm.fullname" . }}-test-connection"
  labels:
    {{- include "headwind-mdm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "headwind-mdm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

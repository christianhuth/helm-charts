---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-for-argocd.fullname" . }}
  labels:
    {{- include "mcp-for-argocd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcp-for-argocd.selectorLabels" . | nindent 4 }}

{{- if not .Values.argocd.auth.existingSecret -}}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mcp-for-argocd.auth.secretName" . }}
data:
  {{- (include "mcp-for-argocd.auth.apiToken.secretKey" .) | nindent 2 }}: {{ .Values.argocd.auth.apiToken | b64enc | quote }}
{{- end }}

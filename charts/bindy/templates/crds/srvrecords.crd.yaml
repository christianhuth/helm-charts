# Copyright (c) 2025 Erick Bourgeois, firestoned
# SPDX-License-Identifier: MIT
#
# This file is AUTO-GENERATED from src/crd.rs
# DO NOT EDIT MANUALLY - Run `cargo run --bin crdgen` to regenerate
#
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: srvrecords.bindy.firestoned.io
spec:
  group: bindy.firestoned.io
  names:
    categories: []
    kind: SRVRecord
    plural: srvrecords
    shortNames:
    - srv
    singular: srvrecord
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.zoneRef
      name: ZoneRef
      type: string
    - jsonPath: .spec.name
      name: Name
      type: string
    - jsonPath: .spec.ttl
      name: TTL
      type: integer
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SRVRecord specifies the hostname and port of servers for specific services. The record name follows the format _service._proto (e.g., _ldap._tcp).
        properties:
          spec:
            description: |-
              `SRVRecord` specifies the location of services.

              SRV (Service) records specify the hostname and port of servers for specific services.
              The name format is: _service._proto (e.g., _ldap._tcp, _sip._udp).

              # Example

              ```yaml
              apiVersion: bindy.firestoned.io/v1alpha1
              kind: SRVRecord
              metadata:
                name: ldap-srv
              spec:
                zoneRef: example-com
                name: _ldap._tcp
                priority: 10
                weight: 60
                port: 389
                target: ldap.example.com.
                ttl: 3600
              ```
            properties:
              name:
                description: |-
                  Service and protocol in the format: _service._proto

                  Example: "_ldap._tcp", "_sip._udp", "_http._tcp"
                type: string
              port:
                description: TCP or UDP port where the service is available.
                format: int32
                maximum: 65535.0
                minimum: 0.0
                type: integer
              priority:
                description: Priority of the target host. Lower values = higher priority.
                format: int32
                maximum: 65535.0
                minimum: 0.0
                type: integer
              target:
                description: |-
                  Fully qualified domain name of the target host.

                  Must end with a dot. Use "." for "service not available".
                type: string
              ttl:
                description: Time To Live in seconds.
                format: int32
                maximum: 2147483647.0
                minimum: 0.0
                nullable: true
                type: integer
              weight:
                description: |-
                  Relative weight for records with the same priority.

                  Higher values = higher probability of selection.
                format: int32
                maximum: 65535.0
                minimum: 0.0
                type: integer
              zoneRef:
                description: |-
                  Reference to a `DNSZone` resource by metadata.name.

                  Directly references a `DNSZone` resource in the same namespace by its Kubernetes resource name.
                type: string
            required:
            - name
            - port
            - priority
            - target
            - weight
            - zoneRef
            type: object
          status:
            description: Generic record status
            nullable: true
            properties:
              conditions:
                default: []
                items:
                  description: |-
                    Condition represents an observation of a resource's current state.

                    Conditions are used in status subresources to communicate the state of
                    a resource to users and controllers.
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status to another (RFC3339 format).
                      nullable: true
                      type: string
                    message:
                      description: Human-readable message indicating details about the transition.
                      nullable: true
                      type: string
                    reason:
                      description: Brief CamelCase reason for the condition's last transition.
                      nullable: true
                      type: string
                    status:
                      description: 'Status of the condition: True, False, or Unknown.'
                      type: string
                    type:
                      description: 'Type of condition. Common types include: Ready, Available, Progressing, Degraded, Failed.'
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                nullable: true
                type: integer
            type: object
        required:
        - spec
        title: SRVRecord
        type: object
    served: true
    storage: true
    subresources:
      status: {}

---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "bindy.fullname" . }}
  labels:
    {{- include "bindy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "bindy.selectorLabels" . | nindent 4 }}

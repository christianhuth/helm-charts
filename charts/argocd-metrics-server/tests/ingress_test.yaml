---
suite: test deployment
templates:
  - deployment.yaml
tests:
  - it: should work
    asserts:
      - isKind:
          of: Deployment
      - equal:
          path: spec.template.spec.containers[0].image
          value: quay.io/argoprojlabs/argocd-extension-metrics:v1.0.3

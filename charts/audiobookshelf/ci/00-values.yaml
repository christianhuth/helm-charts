---
ingress:
  enabled: true
  hosts:
    - host: audiobookshelf.127.0.0.1.nip.io
      paths:
        - path: /
          pathType: ImplementationSpecific
audiobookshelf:
  backup:
    persistence:
      requests:
        storage: 1Gi
  config:
    persistence:
      requests:
        storage: 1Gi
  metadata:
    persistence:
      requests:
        storage: 1Gi
